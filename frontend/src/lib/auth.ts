export function isAuthenticated(): boolean {
  // Aquí iría la lógica real para verificar si el usuario está autenticado,
  // como revisar un token en localStorage o una cookie.
  return true; // Por ahora, siempre devuelve true para propósitos de ejemplo.
}
